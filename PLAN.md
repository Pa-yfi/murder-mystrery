# 🕵️ کارآگاه — نقشه‌ی کامل پروژه (فاز ۱: موتور کامل + ۴۰ پرونده، بدون LLM)

## ۱) معماری
```
karagah/
├── models.py     Player / Case / Evidence / GameState / Enumها
├── roles.py      ۱۸ نقش + تقسیم نقش برای ۴..۱۰ + اعتبارسنجی تعادل
├── cases.py      ۴۰ پرونده‌ی قتل + دک مدارک (هر مدرک ≥۲ تفسیر)
├── dialogue.py   دیالوگ قانون‌محور و قطعی (بدون LLM) از روی راز/استرس/دروغ
├── engine.py     ماشین حالت: لابی→شب→صبح→گفتگو→رای→شبِ بازجویی→صبح (حکم/هیئت)→پایان
├── ui.py         رابط فارسی: صفحه‌ها، کیبورد اینلاین، ایموجی، انیمیشن
└── bot.py        ۶۴ اندپوینت (مستقل از شبکه ⇒ کاملاً تست‌پذیر)
tests/            ۱۸۴ تست سبز ✅
tests/quality/    قراردادهای کیفی — عمداً قرمز (دفترچه‌ی نقص)
```
اتصال به تلگرام = فقط map کردن `bot.handle()` به هندلرهای python-telegram-bot.
لایه‌ی LLM بعداً *فقط* روی `dialogue.py` سوار می‌شود (اینترفیس یکسان).

## ۲) حذف سه‌مرحله‌ای (قلب بازی)
| مرحله | چطور وارد می‌شود | فاصله | چطور خارج می‌شود |
|---|---|---|---|
| 🔦 بازجویی | رای اکثریت گروه | ۱ شب | بازجو سؤال می‌پرسد + سرنخ مبهم می‌گیرد → یا آزادی (تایید بی‌گناهی) یا حبس موقت |
| 🔒 حبس موقت | تایید بازجو | ۲ شب | فقط وقتی **متهم جدیدی وارد بازجویی شده باشد** و بازجو بی‌گناهیِ قبلی را تایید کند |
| ⛓️ حبس ابد | اتمام ۲ شب بدون تبرئه | دائمی | حذف کامل — **نقش فاش نمی‌شود**؛ تا پایان بازی هیچ‌کس نمی‌داند قاتل بود یا بی‌گناه |

**هیئت منصفه:** بعد از یک شب در بازجویی، ۲ بازیکن (یا وکیل به‌تنهایی ⚖️) می‌توانند هیئت منصفه تشکیل دهند؛ ۶۰٪ رای = تبرئه.
**سرنخ‌های بازجو هرگز قطعی نیستند** — بی‌گناهِ پراسترس هم می‌تواند مثل قاتل به نظر برسد.

## ۳) نقش‌های خنثی = ابزار پوشش قاتل
سپر بلا 🎭 (با حبس ابد تنهایی می‌برد)، جانی سریالی 🩸، بقال محله 🏪، قاچاقچی 🚬 — می‌شود گناه را گردنشان انداخت و کارآگاه هم آن‌ها را «مشکوک» می‌بیند.

## ۴) تقسیم نقش (تست‌شده)
۴ن: ۱ | ۵ن: ۱ | ۶ن: ۱ | ۷ن: ۲ | ۸ن: ۲ | ۹ن: ۳ | ۱۰ن: ۳ قاتل — قاتل‌ها همیشه اقلیت، همیشه دقیقاً ۱ بازجو. (از COMPOSITIONS خوانده شده)

## ۵) دسته‌بندی رابط کاربری
- **منوی اصلی:** شروع بازی | بلیتز | دعوت دوستان | داشبورد | آموزش | نقش‌ها | برترین‌ها | فصل | ماموریت‌ها | دستاوردها | پروفایل | پنل ادمین
  («فروشگاه» و «تنظیمات» هرگز ساخته نشدند و در نسخه‌ی ۱.۱ حذف شدند)
- **لابی:** لیست بازیکنان، شمارنده ۴/۸، دکمه‌ی شروع
- **کارت نقش (خصوصی):** ایموجی، تیم، توضیح، اطلاعات محرمانه
- **تابلوی شهر:** 🟢آزاد 🔦بازجویی 🔒حبس موقت ⛓️حبس ابد 💀کشته
- **کارت مدرک:** ۳ تفسیر ممکن + هشدار «هیچ مدرکی به‌تنهایی قاتل را ثابت نمی‌کند»
- **اتاق بازجویی (خصوصی بازجو):** سرنخ‌ها، پرسش، دو دکمه‌ی حکم
- **رای‌گیری / هیئت منصفه:** کیبورد اینلاین + ممتنع
- **انیمیشن‌ها:** شب 🌆🌃🌌🔪 | صبح 🌅📰🚨 | رای 🗳️ | بازجویی 🚪🔦🎙️ | زندان ⛓️🔒 | دادگاه ⚖️📜👨‍⚖️

## ۶) ۳۰ ایده — همه پیاده شد
1 حذف سه‌مرحله‌ای · 2 نقش بازجو با قدرت حکم · 3 سرنخ مبهم (نه قطعی) · 4 هیئت منصفه · 5 وکیل با درخواست تکی · 6 حبس ابد بدون افشای نقش · 7 نقش خنثی سپر بلا · 8 آزادی مشروط از حبس موقت · 9 سکوت کامل نقش‌ها تا پایان · 10 ۴۰ پرونده‌ی یکتا · 11 دک ۶ مدرکی با ۳ تفسیر · 12 دو مدرک گمراه‌کننده در هر پرونده · 13 توییست اختصاصی هر پرونده · 14 تایم‌لاین ۴ مرحله‌ای · 15 موتور استرس · 16 امتیاز اعتماد/سوءظن · 17 دیالوگ قطعی بدون LLM · 18 «tell»های بدنی متهم · 19 اکشن شبانه‌ی نقش‌محور · 20 پزشک/محافظت · 21 همدست با پاپوش‌سازی · 22 خبرچین ضدبازجو · 23 دسترسی اطلاعاتی اختصاصی هر نقش · 24 اعتبارسنجی تعادل تیم‌ها · 25 XP/سکه/رتبه · 26 تابلوی وضعیت زنده · 27 انیمیشن فازها · 28 کیبورد اینلاین کامل فارسی · 29 لاگ بازی برای ریپلی · 30 ۲۴ اندپوینت تست‌شده.

## ۷) گام بعدی (فاز ۲)
LLM اختیاری روی `dialogue.py` · PostgreSQL به‌جای SQLite · بتل‌پس · آنتی‌چیت (مولتی‌اکانت/AFK) · ریپلی تصویری.
(پنل ادمین در نسخه‌ی ۱.۱ تحویل شد و از این فهرست حذف شد.)


## به‌روزرسانی نسخه ۱.۱ (رفع «ربات جواب نمی‌دهد»)
- 🐞 باگ ریشه‌ای: `except KeyError` در dispatch، خطای داخلی هندلرها را «دستور ناشناخته» گزارش می‌کرد. رفع شد؛ حالا هر خطا پیام دوستانه دارد و دستور ناشناخته → منوی اصلی.
- 🐞 دکمه‌های مرده (`rank`/`shop` بدون هندلر) حذف شدند؛ تستِ «هر دکمه به اندپوینت واقعی وصل است» اضافه شد.
- 🐞 تداخل مسیریابی callback: دکمه‌ی بارهٔ `vote` (شروع رای) با `vote:<id>` (ثبت رای) قاطی می‌شد. رفع شد (فقط callbackهای پارامتری نگاشت می‌شوند).
- 🐞 پارامتر `owner` به `Game` اضافه شد؛ `/new` دیگر بی‌صدا شکست نمی‌خورد.
- 🆕 پنل ادمین کاملاً روی **SQLite** (جدول‌های users/games/players/events) با کوئری SQL خالص + قابلیت بن.
- 🆕 صفحه‌ی اول: افزودن به گروه/کانال + دعوت دوست. دیپ‌لینک: میزبان→پنل میزبان، تازه‌وارد→راهنما.
- 🆕 آداپتور تلگرام مقاوم: fallback بدون Markdown، پاسخ به هر دستور ناشناخته، ثبت منوی دستورها در BotFather.
- ✅ ۳۰ تست + شبیه‌سازی زندهٔ تپ روی همه‌ی دکمه‌ها.


## نسخه ۱.۲ — رکورد، گروه، ویرایش درجا
- ♻️ **بازیابی بعد از ری‌استارت:** بازی‌ها در جدول `snapshots` (SQLite) ذخیره و در استارتاپ برگردانده می‌شوند — دلیل اصلی «گم شدن رکورد» همین بود: حافظه با هر ری‌استارت پاک می‌شد.
- 🙋 دکمه‌ی «منم بازی می‌کنم!» روی پیام لابی/پنل میزبان در گروه — ورود با یک تپ.
- ✏️ ویرایش درجا: تپ روی دکمه‌ها همان پیام را ویرایش می‌کند (فلگ `edit`)، چت شلوغ نمی‌شود.
- 🐞 نام خالی → «کارآگاه <id>» (باگ 👤 بی‌نام).
- ⚖️ قوانین: رای به خود ممنوع، قتل خود ممنوع، رای دوباره=ویرایش، مهاجرت میزبانی هنگام خروج میزبان، پیام واضح برای ورود بعد از شروع.
- 🧹 بازی تمام‌شده از حافظه و اسنپ‌شات پاک می‌شود (رفع نشت حافظه/لابی).
  > ⚠️ **در نسخه‌ی ۳.۱ عوض شد:** این حذفِ فوری باعث می‌شد `/end` هیچ‌وقت نقش‌ها را
  > نشان ندهد. حالا بازی تمام‌شده در حافظه می‌ماند (فلگ `finalized`) و فقط
  > اسنپ‌شاتش پاک می‌شود. عارضه: بعد از ری‌استارت، افشای نهایی در دسترس نیست.
- ✅ ۳۸ تست + ممیزی زنده‌ی کل قوانین با ری‌استارت وسط بازی.


## نسخه ۲.۰ — هر ۳۰ ایده پیاده شد (۵۵ اندپوینت، ۶۱ تست)
گیم‌پلی: ۱ تایمر خودکار فاز + tick/JobQueue · ۲ آخرین دفاع متهم · ۳ حالت بلیتز (/blitz) · ۴ رویداد شبانه (قطعی برق/طوفان/شاهد، قطعی و seedدار) · ۵ وصیت‌نامه (/will) · ۶ رای اضطراری شهر ۸۰٪ (/sos) · ۷ نقش کالبدشکاف 🔬 · ۸ زوج سرنوشت (بازی‌های ۷+).
معما: ۹ دفترچه خصوصی (/note /notes) · ۱۰ آزمایشگاه با تاخیر ۲ شب (/lab E1) · ۱۱ رای تفسیر مدرک (/interp E1:0) · ۱۲ راستی‌آزمایی مدرک توسط کارآگاه (/expose) · ۱۳ تناقض‌یاب جواب متهم · ۱۴ بازسازی پرونده در پایان.
رقابت: ۱۵ /top · ۱۶ دقت رای (جدول accuracy + پاداش XP) · ۱۷ MVP · ۱۸ دستاوردها (/achv) · ۱۹ ماموریت روزانه (/missions) · ۲۰ فصل ماهانه (/season).
اجتماع: ۲۱ تا ۳ میز موازی در گروه (/newtable) · ۲۲ تماشاچی بدون اسپویل (/spectate) · ۲۳ رای ناشناس/علنی (/voteanon) · ۲۴ دور دوباره با همان ترکیب (/rematch) · ۲۵ لیگ گروه‌ها (/league).
UX: ۲۶ داشبورد تک‌پیامی ویرایش‌شونده (/dashboard) · ۲۷ کارت نقش PNG با Pillow (/rolecard) · ۲۸ آموزش تعاملی (/tutorial) · ۲۹ هوک صدای فاز (assets/voice/*.ogg) · ۳۰ لایه‌ی دوزبانه (strings.py).


## نسخه ۳.۰ — ۲۰ بهبود کیفیت و تعادل (بر پایه‌ی پژوهش)
منابع: ویکی مافیا/ور‌ولف، مقاله‌ی نظریه‌ی بازیِ ور‌ولف (arXiv 2408.17177)، راهنمای تخصیص نقش MINIFINITI، ویکی Concurrency کتابخانه‌ی python-telegram-bot.

**کیفیت و پایداری ربات:** ۱ قفل هر چت (ضد ریس‌کاندیشن) · ۲ حذف تپ تکراری دکمه (dedup) · ۳ مدیریت کالبک/دستور نامعتبر · ۴ برد قاتل با برابری (parity) طبق قواعد استاندارد · ۵ کول‌داون نقش بین بازی‌ها · ۶ اعتبارسنجی نسبت نقش مخفی ۲۰–۴۵٪ (پژوهش: ~۲۵–۳۰٪) · ۷ audit log ساخت‌یافته در SQL · ۸ پاکسازی ورودی علیه تزریق Markdown · ۹ نرخ‌محدودساز ضد اسپم (فقط محیط واقعی) · ۱۰ ذخیره‌ی اتمیک وضعیت.

**گیم‌پلی و نقش‌ها:** ۱۱ پزشک: نه خودنجاتی، نه دو شب پیاپی · ۱۲ کول‌داون هدف کارآگاه · ۱۳ concurrent_updates با قفل ایمن · ۱۴ وصیت فقط اگر فرصت داشته · ۱۵ نقش شکارچی 🏹 با شلیک آخر · ۱۶ قفل رای بعد از بستن · ۱۷ ترکیب متعادل ۹ و ۱۰ نفره · ۱۸ مسیر برد جانی سریالی (تنها بازمانده) · ۱۹ عدم افشای نقش تا پایان (تثبیت) · ۲۰ مرگ ناگهانی در تساوی رای.

**بازبینی تعادل:** همه‌ی ترکیب‌های ۴..۱۰ اکنون نسبت مخفی ۲۰–۴۴٪ دارند و قاتل‌ها همیشه اقلیت‌اند (مطابق پژوهش). شهرِ ساده به بازی‌های کوچک اضافه شد تا از داغ‌شدن (۵۰٪ مخفی) جلوگیری شود.

**اعداد:** ۵۶ اندپوینت · ۷۷ تست سبز · fuzz تمیز (۵۶×۶ ورودی خراب) · سناریوی زنده‌ی ۹ نفره.


## نسخه ۳.۱ — ۱۰ رفع باگ + ۸ بهبود کیفیت (۶۴ اندپوینت، ۱۸۴ تست)
**۱۰ باگ گزارش‌شده، همه با تست رگرسیون بسته شد:** نشت نقش به گروه هنگام شکست پیوی ·
`ADMIN_IDS` خالی = همه ادمین · ربودن بازیِ در جریان با `/new` · ناپدید شدن `/end` ·
استعلام چندباره‌ی کارآگاه در یک شب · توانایی‌های بی‌اثر (سم/پاپوش/نگهبانی/خبرچینی/مخفی‌کاری) ·
دست‌نیافتنی بودن هیئت منصفه · مهلتِ منقضی در دور دوم رای · ذخیره‌نشدن گذارِ تایمر ·
پیدا نشدن بازی گروه از پیوی.

**۸ بهبود:** پنل اکشن با نام (`/act`) · داشبورد راهنما · آمادگی پیش از شروع (`/ready`) ·
مدرکِ برخاسته از اکشن واقعی · پایانِ قابل‌فهم · بازیکن غایب (`/remind` `/pause` `/resume` `/host`) ·
تعادل بازی (`/balance`) · تست بازیِ کامل.

**بازبینی مستندات (همین نسخه):** متنِ قانون حالا از `config` ساخته می‌شود؛ پیش از این
همه‌جا «۴ تا ۸ نفر» نوشته بود در حالی که بازی از ۴ تا ۱۰ را پشتیبانی می‌کند و لابیِ ۹ نفره
«۹/۸» نشان می‌داد. ارقام هم یکدست فارسی شدند.

### نقص‌های باز (تاییدشده روی همین نسخه)
`tests/quality/` قراردادهای کیفی را نگه می‌دارد و **عمداً قرمز است** — هر شکست یک نقصِ شناخته‌شده است، نه رگرسیون.

| نقص | شرح |
|---|---|
| F-01/02 | بازجویی واقعاً دونفره نیست: `ask()` جواب را خودش تولید می‌کند و به متهم نمی‌رسد؛ `h_ask` هم `private` نیست. |
| F-13 | با زنده بودن جانی سریالی، نبودِ تیم قاتل «برد شهر» اعلام می‌شود. |
| F-15 | شلیک آخر شکارچی فقط در مسیر مرگ شبانه است، نه حبس ابد (برخلاف متن نقش). |
| F-16 | قید «دو شب پیاپی» پزشک با عوض کردن هدف دور می‌خورد؛ حافظه‌ی هدف قبلی کارآگاه سحر پاک می‌شود. |
| F-17 | وکیل، خبرنگار، بقال محله و قاچاقچی در هیچ ترکیبی پخش نمی‌شوند — ۴ نقش از ۱۸ برای بازیکن واقعی دست‌نیافتنی‌اند. |
| F-22 | هر ویرایش رای به `vote_history` اضافه می‌شود و XP دقت را باد می‌کند. |
| F-23 | `is_banned()` نوشته شده ولی در `handle()` صدا نمی‌شود. |
| F-24 | `requirements.txt` نه Pillow دارد (`cards.py`) نه اکسترای job-queue (تایمر). |
| F-11 | `finalized=True` پیش از `record_results()` ست می‌شود؛ شکستِ نوشتن، تلاش دوباره را خفه می‌کند. |
| F-12 | بازیِ تمام‌شده در حافظه می‌ماند ولی اسنپ‌شاتش پاک می‌شود → بعد از ری‌استارت افشا در دسترس نیست. |
